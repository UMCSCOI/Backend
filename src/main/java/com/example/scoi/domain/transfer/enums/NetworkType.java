package com.example.scoi.domain.transfer.enums;

public enum NetworkType {
    TRX, // TRON (TRC-20)
    ETH, // Ethereum (ERC-20)
    SOL, // Solana
    APT, // Aptos
    KAIA // Kaia (구 Klaytn)
}
